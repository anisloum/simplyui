# Single-package repo.
packages:
  - .

allowBuilds:
  # Optional native file watcher; Storybook falls back to polling without it.
  '@parcel/watcher': false
  # esbuild backs both tsup and Vite and needs its postinstall to place a binary.
  esbuild: true
